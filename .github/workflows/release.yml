name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  build:
    # trusted builder: signs attestations, cannot write to the repo
    uses: ./.github/workflows/release-build.yml
    permissions:
      contents: read
      id-token: write
      attestations: write

  publish:
    # publisher: creates the release, cannot sign attestations
    name: publish release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - name: Download release assets
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-assets

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "${GITHUB_REF_NAME}"
          -R "${GITHUB_REPOSITORY}"
          --verify-tag
          --title "${GITHUB_REF_NAME}"
          --generate-notes
          "llingr-rs-nexus_${GITHUB_REF_NAME}.tar.gz"
          "llingr-rs-nexus_${GITHUB_REF_NAME}.tar.gz.sigstore"
          "llingr-rs-nexus_${GITHUB_REF_NAME}.cdx.json"
          "llingr-rs-nexus_${GITHUB_REF_NAME}.spdx.json"
          llingr-nexus-*.crate
